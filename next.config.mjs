/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/Experimental-Homepage',
  assetPrefix: '/Experimental-Homepage/',
  images: {
    unoptimized: true,
  },
}

export default nextConfig
